{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "python run.py",
    "build": "echo Python backend - no build step"
  }
}
